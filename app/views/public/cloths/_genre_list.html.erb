<div class = "col-2">
  <table class="table table-borderd table-hover mt-5">
    <tr style="background-color: #000033 ;text-align:center " >
      <th style="color:white; ">衣類ジャンル検索</th>
    </tr>
    <% genres.each do |genre| %>
    <tr>
      <td class="text-center"><%= link_to "#{genre.name}", genre_search_path(id: genre.id) %></td>
    </tr>
    <% end %>
  </table>
</div>
